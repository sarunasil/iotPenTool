#example interface blueprint

tool:
  name: tool_name
  version: tool_version
  command: tool_call_name
  arguments:
    flags:
      name:
        flag: flag
        description: description
    values:
      - value_without_flag
